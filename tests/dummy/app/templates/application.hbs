<Table
    @columns={{this.columns}}
    @rows={{this.data}}
    @hasMoreRows={{this.hasMore}}
    @isLoading={{this.isLoadingModels}}
    {{!-- @loadMoreRows={{action "loadMoreModels"}}
    @updateSorts={{action "updateSorts"}}
    @onRowClick={{action "transitionToTransaction"}} --}}
    @enableSort={{true}}
    @fillMode="nth-column"
    @fillColumnIndex={{3}}
/>

<h1>Testing</h1>
import type { Plugin } from 'rollup';
export declare class Addon {
    #private;
    constructor(params?: {
        srcDir?: string;
        destDir?: string;
    });
    appReexports(patterns: string[], opts?: {
        mapFilename?: (fileName: string) => string;
    }): Plugin;
    publicEntrypoints(patterns: string[]): Plugin;
    hbs(): Plugin;
    clean(): Plugin;
    keepAssets(patterns: string[]): Plugin;
    output(): {
        dir: string;
        format: string;
        entryFileNames: string;
    };
    dependencies(): Plugin;
}
